<tbody>
  <tr>
    <th scope="row"><%= friend.email %></td>
    <td><%= friend.first_name %></td>
    <td><%= friend.last_name %></td>
    <td>
      <%= link_to 'Unfollow', friendship_path(friend), method: :delete, class: "btn btn-sm btn-warning", data: {confirm: "Are you sure you want to untrack"} %>
    </td>
  </tr>
</tbody>