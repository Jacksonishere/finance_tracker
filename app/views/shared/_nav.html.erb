<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <%= link_to 'FinanceTracker', root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 flex-grow-1 align-items-center">
        <li class="nav-item">
          <% if request.path == portfolio_path %>
            <%= link_to 'Portfolio', portfolio_path, class:"nav-link active", aria: { current: "page" } %>
          <% else %>
            <%= link_to 'Portfolio', portfolio_path, class:"nav-link", aria: { current: "page" } %>
          <% end %>
        </li>

        <li class="nav-item">
          <% if request.path == friends_path %>
            <%= link_to 'Friends', friends_path, class:"nav-link active", aria: { current: "page" } %>
          <% else %>
            <%= link_to 'Friends', friends_path, class:"nav-link", aria: { current: "page" } %>
          <% end %>
        </li>

        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Profile
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <li>
              <%= link_to edit_user_registration_path, class: "dropdown-item" do %>
                <%= fa_icon 'edit' %> Edit Profile
              <% end %>
            </li>
            <li><hr class="dropdown-divider"></li>
          </ul>
        </li>
        <li class="nav-item">
          <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item ms-auto me-4">
           <span><%= fa_icon 'user', class: "me-2" %><%= current_user.fullname %></span>
          </li>
          <li class="nav-item me-2">
            <%= link_to 'Logout', destroy_user_session_path, class: "nav-link btn btn-danger", tabindex:"-1", aria:"true", style: "color: white", method: :delete %>
          </li>
        <% end %>
      </ul>
      <form class="d-flex">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>